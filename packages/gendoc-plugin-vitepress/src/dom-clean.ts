import type { CheerioAPI } from 'cheerio';

/** 移除 VitePress chrome / 装饰节点（Task 3 补全） */
export function cleanVitePressContent($: CheerioAPI, $content: ReturnType<CheerioAPI>): void {
  // no-op until Task 3
  void $;
  void $content;
}
