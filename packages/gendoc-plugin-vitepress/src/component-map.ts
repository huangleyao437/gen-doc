import type { CheerioAPI } from 'cheerio';

/** 映射 custom-block / code-group / badge（Task 3 补全） */
export function mapVitePressComponents($: CheerioAPI, $content: ReturnType<CheerioAPI>): void {
  void $;
  void $content;
}
