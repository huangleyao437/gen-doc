import type { CheerioAPI } from 'cheerio';

/**
 * 移除 Sphinx 内容根内的 chrome / 装饰 UI（Task1 占位，后续任务实现）。
 */
export function cleanSphinxContent($: CheerioAPI, $content: ReturnType<CheerioAPI>): void {
  void $;
  void $content;
}
