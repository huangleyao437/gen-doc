import type { CheerioAPI } from 'cheerio';

/**
 * 将 Sphinx 组件映射为语义化 HTML，便于 turndown（Task1 占位，后续任务实现）。
 */
export function mapSphinxComponents($: CheerioAPI, $content: ReturnType<CheerioAPI>): void {
  void $;
  void $content;
}
